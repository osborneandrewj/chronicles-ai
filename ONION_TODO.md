# Onion Architecture Refactor — Progress Tracker

Living checklist for the refactor specified in [`ONION_ARCH_REFACTOR.md`](./ONION_ARCH_REFACTOR.md).
**Branch:** `onion-arch-refactor` (preview branch — may be discarded).

> **For agents:** Read the matching section of `ONION_ARCH_REFACTOR.md` before editing. After completing a
> checklist item, change its `[ ]` to `[x]` and append a one-line note `— <what/where>`. After each phase,
> run the phase **Gate** and record the result in the Status Log at the bottom. Never mark a box `[x]`
> until its gate command actually passes — paste the real output, don't assert.

## Status legend
`[ ]` not started · `[~]` in progress · `[x]` done & gate-verified · `[!]` blocked / needs Andrew

## Baseline (captured 2026-06-04, before any move)
- `npm run type-check` → clean
- `npm test` → **33 files / 323 tests pass**, ~2.3s
- These are the regression reference for every "no behavior change" phase (P0, P1, P4, P5).

---

## Phase order & dependency spine
`P0 → P1 → {P2 → P3}  ∥  {P4 → P5 → P6} → P7`
P2/P3 (Mongo) and P4/P5/P6 (carve+split) can run in parallel **after P1**.

| Phase | Goal | Behavior change | Autonomous? |
|---|---|---|---|
| P0 | Monorepo skeleton, no logic moved | No | ✅ |
| P1 | Repository ports over SQLite (strangler-fig) | No | ✅ |
| P2 | Mongo + Mongoose adapter behind flag (default off) | No | ✅ (code only) |
| P3 | Backfill + dual-read verify + **prod cutover** | Storage | ❌ **manual gate** (prod DB + hosting decision) |
| P4 | Carve domain services out of god files | No | ✅ |
| P5 | Thin `chat/route.ts` into `AdvanceTurn` | No | ✅ |
| P6 | Extract client into `apps/web` + `packages/contracts` | Topology | ✅ |
| P7 | CI boundary enforcement; **delete SQLite** | No | ⚠️ tooling ✅ / deletion ❌ (after P3 soak) |

---

## P0 — Monorepo skeleton (no logic moves)
Spec: §2.3, §5.1-P0. **Goal:** move the app under `packages/server` verbatim; stand up workspaces; keep build/test green.

- [x] Root `package.json`: `"workspaces": ["packages/*", "apps/*"]`, `private: true`, root scripts delegate to workspaces — root scripts now `npm -w @chronicles/server run <x>`; app deps moved out of root
- [x] `tsconfig.base.json` at root with shared compilerOptions (`composite`, `declaration`, strict, bundler resolution) — created at repo root; server tsconfig `extends` it
- [x] Move `src/`, `tests/`, `prompts/`, `next.config.ts`, `vitest.config.ts`, `eslint.config.mjs`, tailwind/postcss config → `packages/server/` — `git mv` (renames, history preserved); also `scripts/`, `next-env.d.ts`, `tsconfig.json`
- [x] `packages/server/package.json` (`@chronicles/server`) with current deps; `@/*` alias → `packages/server/src/*` (unchanged for now) — name `@chronicles/server` v0.6.21; `@/* → ./src/*` retained in server tsconfig
- [x] Fix cwd coupling: `prompt-files.ts` + `db.ts` resolve via `import.meta.url`, not `process.cwd()` (prereq for everything after) — `path.dirname(fileURLToPath(import.meta.url))` (the `new URL(literal, import.meta.url)` form broke `next build` by webpack-rewriting the .sqlite/.md into asset modules); `DATABASE_PATH` override + `NEXT_PHASE` `:memory:` guard preserved; verified from a foreign cwd
- [x] Keep `serverExternalPackages: ['better-sqlite3']` in server `next.config.ts` — moved verbatim, intact
- [x] Document version-of-record (still `packages/server/package.json` until P6) — v0.6.21 lives in `packages/server/package.json`; `src/app/page.tsx` (now `packages/server/src/app/page.tsx`) still reads its `pkg.version`
- **Gate:** `npm run build && npm test && npm run type-check` green from repo root via workspace scripts. — type-check clean; `npm test` 33 files / 323 tests (322 pass, 1 pre-existing skip); `npm run build` succeeds (benign "TypeScript project references not fully supported" warning, falls back to incremental)

## P1 — Repository ports over SQLite (strangler-fig)
Spec: §3.4, §5.1-P1. **Highest-leverage, lowest-risk. Must precede Mongo.** Wrap SQL only — do NOT extract decisions (that's P4).

- [x] Define `domain/ports/`: all 14 ports + Clock + Logger present in `packages/server/src/domain/ports/` (`index.ts` barrel)
- [x] **All ports `async`** — every method returns a `Promise`; SQLite adapters wrap sync calls in `Promise.resolve`
- [x] `TurnRepository` is append-only: `insert`/`recentTurns`/`turnsBefore`/`latestUserTurnId` + `mergeMetadata(turnId, agentKey, block)` + `incTtsChars(worldId, turnId, n)`; **no general `update`/`setMetadata`** — verified in `domain/ports/turn-repository.ts`
- [x] `Clock` port present (`now()`/`today()`); `cost-cap` reads today via `clock.today()`. `[~]` remaining `datetime('now')` is still emitted inside the SQLite adapter/db.ts (allowed per spec §5.1-P1 — interface exists; full app-level routing is incremental)
- [x] Single SQLite impl in `infrastructure/persistence/sqlite/` implementing all ports (14 `*.sqlite.ts` + unit-of-work)
- [x] `composition/container.ts` wires adapters (the only infra importer)
- [~] Migrate `import { db }` / named-fn importers off `@/lib/db` — **partial.** Done: `cost-cap.ts` (fully strangled → container). Migrated route handlers from prior P0/P1 work (turns/usage/tts/world-correction(s)/play page) use the container. **Remaining VALUE importers** (SQL-owning lib modules whose SQL is already wrapped behind adapters that delegate to them, + the P5 god endpoint): `archivist.ts`, `intent-reconciler.ts`, `meta-commands.ts`, `npc-agent.ts`, `npc-intents.ts`, `npc-promotion.ts`, `opening-turn.ts`, `place-resolver.ts`, `reveries.ts`, `world-state.ts`, `worlds.ts`, `app/api/chat/route.ts`. Type-only db.ts imports remain in ports + a few libs (row types live in db.ts until P4 moves them to `domain/entities/`).
- [x] `cost-cap.ts` `SUM(json_extract(...))` → `UsageRepository.todaysTokenTotal(clock.today())` (SQL lives in `usage-repository.sqlite.ts`); `cost-cap.ts` now imports the container, not `db` — fully strangled. Callers (`chat/route.ts`, `opening-turn.ts`, `cost-cap.test.ts`) made async.
- [x] `import "server-only"` on every infra/repo module + container (verified present)
- [~] **Gate:** full suite green (323). Guard-grep test NOT added — strangling is not complete (SQL-owning lib modules + the chat god endpoint still import `db`), so the `zero @/lib/db outside sqlite` invariant does not yet hold. Adding the guard now would fail. Deferred until the remaining importers land (overlaps P4 SQL-relocation + P5 AdvanceTurn carve).

## P2 — Mongo + Mongoose adapter behind a flag
Spec: §4 (whole), §5.1-P2. Default `PERSISTENCE=sqlite`. Mongoose forbidden outside `infrastructure/persistence/mongo/`.

- [x] Add `mongoose` dep to `packages/server` — `mongoose@^9.6.3` (dependency) + `mongodb-memory-server` (devDependency); `npm install` at root
- [x] `infrastructure/persistence/mongo/connection.ts` (replica-set guard + build-phase no-op replacing `NEXT_PHASE :memory:`) — `connectMongo` probes `hello` for `setName`/`isdbgrid` and throws on a standalone; `isBuildPhase()` returns a disconnected `createConnection()` during `phase-production-build` (no dial)
- [x] Mongoose models for 15 top-level collections + 2 embedded subdocs (§4.2); enums for CHECKs, `min/max` ranges, `nameKey`/`titleKey` normalized unique indexes — `mongo/models/index.ts` (THE ONLY mongoose-import home): worlds(+initialState)/turns/characters/places(+geo+profile)/scenes/npc_reveries/npc_intents/story_threads|clues|objectives|resources/timeline_events/population_templates/place_occupancy_snapshots/tts_audio_cache/world_corrections + `counters`; CHECK→`enum`, `importance 1..5`/`intensity 0..1`→`min/max`, `lower(name)`→`{worldId,nameKey}`/`{worldId,titleKey}` unique
- [x] `counters` collection + atomic `$inc` for monotone turn `seq` (§4.5) — never `ObjectId` for ordering — `MongoContext.nextSeq('turnSeq')` via `findOneAndUpdate $inc` (session-aware); every collection's integer `id` allocated the same way so ports stay `number`-typed; `[t:N]` provenance preserved
- [x] `TurnRepository` mongo impl: append-only; `mergeMetadata` via `$set` nested path, `incTtsChars` via `$inc` (§4.4) — never clobber — `turn-repository.mongo.ts` exposes only `insert`/metadata ops; no general `update`/`setMetadata`/`delete`; guard test asserts content/role/seq untouched after insert
- [x] `UnitOfWork` mongo impl via `session.withTransaction`; fail-fast at boot if no replica set — `mongo-unit-of-work.ts` threads the session through `MongoContext.setSession` so the seq counter + dependent insert commit atomically; replica-set fail-fast lives in `connectMongo`
- [x] Port the **prune** logic (npc_reveries cap 3, occupancy retention) into the use-case-run transaction, not just inserts — reverie cap-3 + dedup + repoint-before-delete in `reverie-repository.mongo.ts` (session-scoped); tts retention prune in `tts-cache-repository.mongo.ts` runs in the same `withTransaction` as the upsert. (Occupancy snapshots are append-only in the SHIPPED SQLite path — no prune exists there — so the mongo insert stays append-only to keep the stores byte-identical; flagged in code.)
- [x] Composition-root selection: `PERSISTENCE=sqlite|mongo` (default sqlite) — `container.ts` reads `PERSISTENCE`; sync `getContainer()` for sqlite default, async `initContainer()` for mongo (connect+index build via dynamic import so the sqlite path never loads mongoose); downstream is store-agnostic
- [x] `MemoryRepository.searchSimilar` port + no-op `[]` adapter (Phase-2 vector slot, §4.8) — `domain/ports/memory-repository.ts` + `SqliteMemoryRepository`/`MongoMemoryRepository` both return `[]`; wired into the container for both stores
- **Gate:** full Vitest suite passes with `PERSISTENCE=mongo` against `MongoMemoryReplSet`; default run still sqlite & green. — **GREEN.** type-check clean; default `npm test` = 361 pass + 1 skip (byte-green, sqlite default, untouched); `npm run test:mongo` = 16 pass against a REAL `MongoMemoryReplSet` (MongoDB 8.2.6 single-node replica set; covers unique-index E11000, seq monotonicity under concurrent inserts, metadata `$set`-nested additive merge, `$inc` tts.chars, append-only guard, reverie cap-3 + tts retention prune, UnitOfWork rollback + atomic commit, replica-set guard, build-phase no-op); `npm run lint` clean (0 errors); `npm run build` ✓ (mongoose does not leak into the client bundle). Mongo suite is isolated via `vitest.mongo.config.ts` + excluded from the default run so the SQLite gate stays byte-identical.

## P3 — Backfill, dual-read verify, cutover  ❌ MANUAL GATE
Spec: §4.9, §5.1-P3. **Touches prod DB + needs a hosting decision. Autonomous work = scripts only; execution is Andrew's.**

- [x] **[code]** `scripts/migrate-sqlite-to-mongo.ts` (idempotent; integer `turns.id`→`seq`; seed `counters`; `createIndexes()` after bulk insert) — reuses P2 `connectMongo`+`buildModels` (no hand-rolled schema, no direct `mongoose` import); replaceOne(upsert)-by-id is idempotent; folds `place_profiles`→`places.profile`, `osm_*`→`geo`, TEXT-JSON (`initial_state_json`/`metadata`/`traits_json`/`daily_loop`)→native subdocs, `match_tags`→`matchTagsJson`, BLOB→Buffer; seeds all 16 monotone counters at `MAX(id)` via `$max` (never regresses on re-run); `createIndexes()` after bulk insert. CODE ONLY — not executed.
- [x] **[code]** `scripts/verify-parity.ts` (row counts, `MAX(seq)` parity, sampled deep-equal incl. `getFullWorldState`) — read-only on both stores; per-collection exact counts, full turn id↔seq continuity array equality + `MAX(seq)` parity + `counters.turnSeq ≥ MAX`, N-sampled deep-equal on turns AND characters (N via `VERIFY_SAMPLE_N`/argv, default 25), and a store-agnostic per-world `getFullWorldState`-shape assembly (worldTime/currentSceneId + ordered character/place/scene/thread ids) compared from both; PASS/FAIL summary, non-zero exit on any diff. CODE ONLY — not executed.
- [ ] `[!]` **Andrew:** choose Mongo hosting — Atlas vs Railway single-node replica set (Open Q #2; transactions + future `$vectorSearch` depend on it)
- [ ] `[!]` **Andrew:** back up `/data/chronicles.sqlite` (+wal/shm) from prod via `railway ssh`
- [ ] `[!]` **Andrew:** run backfill against backup → run parity script until clean
- [ ] `[!]` **Andrew:** quiet-window cutover: tail backfill, flip `PERSISTENCE=mongo`, redeploy, smoke a streamed turn
- **Gate:** parity script clean on prod snapshot; one turn streams end-to-end on Mongo in browser. Rollback = flip env back + redeploy.

## P4 — Carve domain services out of the god files
Spec: §3.3, §5.1-P4, §5.2. **Characterization tests FIRST** (archivist ordering has prod-bug history).

- [x] Characterization tests for `NameResolution` / `SceneTransition` / `applyArchivistPatch` ordering (golden patches → expected merge plans), seeded from known prod-bug scenarios — `tests/name-resolution.test.ts` (20 golden-rule tests: Marcus⊂Marcus Reeves, Jordana freshest, descriptor reveal via aliases, generic-room→residential) + `tests/scene-transition.test.ts` (8 decisions seeded from world-13 teleport + Call-In turn-403 home-flip); the pre-existing `archivist.test.ts` already pins `applyArchivistPatch` ordering + merge outcomes end-to-end and stayed byte-green through every extraction
- [x] **Row TYPE defs → `domain/entities/`** (P4 foundation, step 1) — World/Turn+metadata/Character/Place/Scene/Story*/NpcIntent/Reverie/Occupancy/Usage/TtsCache/Correction now in `domain/entities/*`; 6 ports re-pointed off `@/lib/db` → `@/domain/entities` (last domain→infra type leak gone); `db.ts`/`world-state.ts`/`worlds.ts`/`npc-intents.ts`/`reveries.ts` re-export for back-compat
- [x] **Low-risk drop-in moves first** (zero behavior change) — `ReverieFlare`(reverie-flare.ts), `WorldClock`(world-clock.ts), `OccupancySim`(occupancy-sim.ts), `CharacterDedup`(character-dedup.ts), `MemorableFactProvenance`, `StorySignal`, `ActionClassifierRules`(action-classifier-rules.ts), `TurnNumbering`(turn-numbering.ts), + `narrator-guidance.ts` (moved as-is) → `domain/services/`. Split files keep CRUD/I-O (reveries) + orchestrator (place-population `buildPlaceOccupancySnapshot`) + LLM fallback (classifier, rules-first preserved) behind. Old `@/lib/*` modules re-export moved members. `domain/` imports none of the forbidden modules; `character-dedup` still imports pure `@/lib/character-identity` (not banned).
- [x] `model-registry.ts` consolidation — `infrastructure/llm/model-registry.ts` (server-only) is now the sole home for `NARRATOR_MODEL='grok-4.3'` + `HAIKU_MODEL='claude-haiku-4-5-20251001'`; `pricing.ts` moved to `infrastructure/llm/pricing.ts` (server-only, RATES keyed by registry consts); `lib/pricing.ts` slimmed to client-safe `formatUsd`; `turn-cost.ts` reads cost math from infra; all 8 call sites (6 Haiku agents + 2 narrator) import from the registry — zero model-ID literals remain outside it. Production build verified (server-only does not leak into Chat.tsx).
- [x] `PatchSanitizer` (sanitize + transition guards + transit normalize + deterministic patch) → pure domain service — `domain/services/patch-sanitizer.ts` (`sanitizeArchivistPatch`, `extractDeterministicPatch`, `normalizeTransitPlaceName`/`normalizeTransitPlacesInPatch` + all pure helpers); purest, moved first (commit `68ff9d1`). `archivist.ts` imports + re-exports for back-compat. No I/O.
- [x] `SceneTransition` invariant → returns open/close **intents**, not UPDATEs — `domain/services/scene-transition.ts` `decideSceneTransition()` returns a `SceneTransitionIntent {placeId, reason, priorScenePlaceId}` | null; the txn reads scene/player place ids, asks the pure service, applies the intent (auto-close→open "Arriving at …"→drag player). Own commit `9155971` (separate from merge path, Open-Q#5). Both warn payloads preserved.
- [~] `NameResolution` (resolve/merge/alias/freshest) → returns a **MergePlan**, issues no SQL — **PARTIAL** (commit `d02d951`): the pure deciding RULES are extracted to `domain/services/name-resolution.ts` (`placesMatch`/`charactersMatch`/`isAmbiguousCharacterMatch`/`freshest`/`chooseLonger`/`mergeLineBlocks`/`strongest*`/`maxNullable`/`filterAliasesAgainstName`/`findCharacterByNameOrAlias`/`canonicalCharacterKey` + word sets), no SQL. **DEFERRED:** the full "return a MergePlan {canonicalId, mergeOps, aliasUpdates}, issue no SQL" carve — `resolveCharacter`/`mergeCharacters`/`runAliasMerges`/`mergePlaces` still issue UPDATE/DELETE/RETURNING **mid-loop** inside the 370-line `applyArchivistPatch` txn (they read rows fresh per call to see same-patch writes). Converting to a returned-plan requires the use case to load-all → simulate → apply, which is the P5 `AdvanceTurn` rewrite of the fused transaction (prod-bug history). Reverting risk > reward on a "no behavior change" pass; staged for P5. See itemsRemaining.
- [~] `NpcPromotion` tiering → returns write commands from a snapshot — **PARTIAL** (commit `7005a41`): pure tiering RULES extracted to `domain/services/npc-promotion.ts` (`nextAgencyTier`, `isTransientServiceNpc`, `AUTO_PROMOTE_THRESHOLD`), no SQL. **DEFERRED:** `recordAppearancesAndAutoPromote` still issues per-character bump/promote/demote UPDATEs interleaved with the tier decision inside one txn; the "snapshot → command list the use case applies" carve is the same fused-transaction rewrite as NameResolution, staged for P5.
- [x] Narrator-markdown renderers (`formatStateBlock`/`formatDossierBlock`/...) → `server/render`, NOT domain — `src/server/render/state-block.ts` (`formatStateBlock`/`formatDossierBlock`/`formatOccupancyBlock`/`formatPlaceGeo` + private helpers + `NpcPlannedAction`/`ReverieRenderContext` types). `server-only`; `world-state.ts` re-exports. Commit `f3e06ff`. Build verified (no client bundle leak).
- [ ] (deferred PR) Generalize overfit `minerva/caesar/maya/usace` constants — behavior change, separate PR
- **Gate:** characterization tests + full suite green; no behavior change vs baseline.

## P5 — Thin `chat/route.ts` into `AdvanceTurn`
Spec: §3.5, §5.1-P5. **Preserve fail-open vs fail-closed semantics and the `dbTurnId` flush ordering.**

- [x] `application/use-cases/advance-turn.ts`: `advanceTurn({worldId, playerText, incomingMessages})` → `AdvanceTurnResult` (canned meta/replay | `{kind:'stream', stream: NarrationStream<number|undefined>}`); no framework `onFinish` in app code — the SDK lives in `infrastructure/narrator/narrate-turn.ts` (the NarratorPort adapter, route-wired as `buildNarration`)
- [x] ~20 captured closure vars → explicit use-case state — the gates, the pure `decideReplay` dedup decision, the pre-stream player-turn persist (via `TurnRepository.insert`), and `[t:N]` `playerTurnId` are now explicit use-case state; the dense SQL+SDK pipeline body (classify/geocode/npc-agent/occupancy/reverie/prompt render + post-stream factual work) is carved into the injected `buildNarration` infra step (its own further granular decomposition tracked below)
- [x] Pre-stream fail-closed gates → domain errors — `WorldNotFoundError`→404, `EmptyPlayerActionError`→400, `BudgetExceededError {used,limit}`→429, mapped to HTTP ONLY at the route edge
- [x] Post-stream work stays **best-effort** (`console.error`+continue): reconciler, archivist, promotion, dedup, reverie, npc-agent, occupancy, geocoder — all preserved verbatim in `narrate-turn.ts`; NOT converted to hard errors (use-case test asserts archivist-throws → both turns still persist)
- [x] SIGTERM in-flight-archivist drain → `BackgroundTasks` port (`domain/ports/background-tasks.ts`) + `ProcessBackgroundTasks` infra adapter (owns `process.once('SIGTERM')` + drain) in composition root; the narrate adapter `register`s the archivist promise
- [x] `chat/route.ts` → thin adapter (593→~160 LOC): parse `messages[]`→`playerText` → `advanceTurn` → render canned | pipe `chunks` + append `dbTurnId` after `completion` (extracted `appendDbTurnId` helper, unit-tested for last-arrival ordering); world-existence-before-body-parse 404 ordering preserved at the edge
- [~] Repeat thin-adapter treatment: `world-correction→ApplyCorrection`, `turns→LoadHistory`, `world-state→InspectWorld`, `usage→SummarizeUsage`, `tts→SynthesizeNarration`, `tts/record→RecordTtsUsage`, corrections list — **P5 part 1 DONE for the lower-risk routes:** `turns→LoadHistory`, `usage→SummarizeUsage`, `world-state→InspectWorld`, `world-corrections→ListCorrections`, `tts→SynthesizeNarration` (+ new `SpeechSynthesizer` port + `XaiSpeechSynthesizer` infra adapter + container wiring), `tts/record→RecordTtsUsage`, `world-correction→ApplyCorrection` (PARTIAL — orchestration carved; the fused `applyArchivistPatch` merge txn + LLM extractor injected as route-wired fns, staged for AdvanceTurn). All in `application/use-cases/`; `application/` imports `@/domain/*` + `node:crypto` only (grep clean — no SQL/SDK/next/lib/infra). `chat→AdvanceTurn` is P5 part 2.
- [x] Use-case tests against in-memory fake repos; assert archivist-throws → turn still persists — `tests/advance-turn.test.ts` (8 tests: fail-closed budget/world/empty gates persist nothing; fail-open archivist-throws → player+narrator turns persist + completion resolves the id; `decideReplay` pure branches) + `tests/chat-stream-metadata.test.ts` (3 tests: `dbTurnId` arrives LAST / absent on empty / waits for slow completion)
- [~] **Gate:** full suite green; integration test asserts `dbTurnId` metadata part arrives last — **type-check + tests + build GREEN** (361 pass + 1 skip; metadata-last test green). Browser end-to-end smoke is the manual exit criterion (not run here — needs a live xAI key + dev server). **DEFERRED (tracked):** the fused `applyArchivistPatch` merge txn (the NameResolution/NpcPromotion MergePlan from P4) is still applied inline by the injected narrate adapter, not yet a load-all→simulate→apply plan; and `buildNarration` is one injected infra step rather than fully decomposed into granular per-step use-case deps. Both are net-zero-behavior and reverting-risk > reward on this slice; carried forward.

## P6 — Extract client into `apps/web` + `packages/contracts`
Spec: §2.4-§2.8, §5.1-P6. Client carries **no onion concern**.

> **P6 status: LOGICAL split done & green; PHYSICAL `apps/web` move deferred.** The contracts seam, shared
> splitter, and server-side derivation landed green (`f87a042`, `d1673bf`). The physical repackaging into a
> separate `apps/web` Next host (`[~]` items below) stalled a workflow agent in a `next build` fix-loop — it's
> Next-15-monorepo packaging, not architecture, and is safe as an interactive follow-up. P7's dependency-cruiser
> enforces the client→contracts-only boundary *today* by path-scoping `packages/server/src/components`, so the
> deferral loses no enforcement value.

- [x] `packages/contracts` (`@chronicles/contracts`, dep: zod only): DTOs + `ChatRequestSchema` + `MessageMetadata{dbTurnId}` — `f87a042`
- [x] `packages/contracts/src/pure/sentence-splitter.ts` — shared, byte-identical client↔server; `sentence-splitter.test.ts` green — `f87a042`
- [~] `apps/web` (Next.js): **DEFERRED** — client components still under `packages/server/src/components` but already import only contract DTOs; physical host extraction is the follow-up that stalled
- [x] Move cost/pricing fully server-side; client keeps only `formatUsd` — `turn-cost.ts` thinned, cost flows via `TurnCostDTO` — `d1673bf`
- [x] Move render-only domain server-side: badges / `organizePlayerProfileFacts` / `[t:N]` derivation → DTO ships ready-to-render fields — `d1673bf`
- [~] Re-express SQL-reading Server Components + Server Actions as HTTP endpoints — **DEFERRED** with the physical move (work in-process today)
- [~] Per-package tsconfigs + project references; drop root `@/*` alias — **DEFERRED** with the physical move
- [~] Version-of-record `apps/web/package.json` (Open Q #6) — moot until `apps/web` exists; stays `packages/server` for now
- **Gate (partial):** type-check + 361 tests + `npm run build` green with the logical split in place; physical-move gate deferred.

## P7 — Enforce boundaries in CI; delete SQLite
Spec: §2.5, §3.7, §5.1-P7.

- [x] `.dependency-cruiser.cjs` at root with the §2.5/§3.7 forbidden ruleset; wired into CI via root `pretest` — `dependency-cruiser@^16.10.4` (root devDep); `npm run depcruise` + `pretest` hook run it before vitest. 11 forbidden rules: domain-points-inward, domain-no-io-or-framework, app-imports-domain-only, infrastructure-only-via-composition, mongoose-only-in-mongo-adapter, better-sqlite3-only-in-sqlite-adapter, model-registry-not-in-domain-or-app, client-no-server-layers, client-no-native-or-server-sdk, contracts-pure, no-circular. Path-scoped to the ACTUAL layout (client tree = `packages/server/src/components`, P6 physical move deferred). `tsConfig: tsconfig.depcruise.json` (minimal alias-only) resolves `@/*`+`@chronicles/*`; `tsPreCompilationDeps: false` (runtime imports only — erased `import type` cannot leak a bundle/cycle). Exit 0; non-vacuity verified (temp domain→composition import fired `domain-points-inward`).
- [x] Guard-grep tests (`tests/architecture-boundaries.test.ts`, 6 tests, in default suite): no model-ID literal (`claude-`/`grok-`) outside `infrastructure/llm/` (ZERO exceptions); no `import 'mongoose'` outside `infrastructure/persistence/mongo/` (ZERO); no NEW merge/name-resolution deciding branch under `infrastructure/` (ZERO); `better-sqlite3` only in sqlite adapter + pinned deferred-carve allowlist (`lib/db.ts`/`migrations.ts`/`archivist.ts`); every infra module + route under `server-only`. Comments stripped before matching; non-vacuity verified (temp `claude-…` literal fired the model-ID test).
- [x] `import "server-only"` on every infra module + route handler — all 14 infra dirs already carried it; ADDED to all 8 API route handlers (chat/usage/world-corrections/tts/turns/world-correction/world-state/tts/record). `mongo/test-support.ts` deliberately NOT marked (test-harness-only, never client-shipped, lives in mongo dir solely to confine `mongoose`); excluded in the guard.
- [ ] `[!]` **after P3 prod soak:** delete SQLite adapter + `migrations.ts` + `better-sqlite3` dep; drop `serverExternalPackages` — UNCHANGED (Andrew's gate; tooling phase does NOT delete the adapter)
- **Gate:** dependency-cruiser + guard tests pass in CI — **GREEN.** type-check clean; `npm test` = 367 pass + 1 skip (361+1 baseline + 6 new guard tests), depcruise runs first clean via `pretest`; `npm run depcruise` exit 0; `npm run test:mongo` = 16 green; `npm run build` ✓. (Deferred: build with SQLite removed — after prod soak.)

**P7 itemsRemaining (tracked deferred carves, encoded as narrowly-scoped allow-exceptions — NOT rule relaxations):**
- `infrastructure-only-via-composition` exempts (a) `infrastructure/llm` (the model-ID + pricing centralization home the 8 LLM-calling `lib/*` agents import BY DESIGN — the grep guard then forbids model-IDs everywhere else) and (b) `app/api/chat/route.ts` (P5-deferred: it injects `narrateTurn` directly instead of via the container; wiring the NarratorPort into `composition/` is the AdvanceTurn follow-up).
- `better-sqlite3` deferred-carve allowlist: `lib/db.ts`, `lib/migrations.ts`, `lib/archivist.ts` (SQL-owning lib the adapters still delegate into — P1/P4 strangling incomplete). New importers trip both the cruiser rule and the grep test.
- `tsPreCompilationDeps: false` drops the three known erased domain↔lib type back-edges (`patch-sanitizer`/`name-resolution` → `archivist`, `state-block` → `world-state`); the carve relocating `ArchivistPatch`/`CharacterRow`/`PlaceRow`/`NarratorWorldState` into `domain/entities` removes even the type edge.
- Client tree (`packages/server/src/components`) still VALUE-imports a few pure client-safe helpers from `@/lib` (`formatUsd`, `SLASH_COMMANDS`, `organizePlayerProfileFacts`, `deriveCharacterBadges`/`deriveSceneBadge`); the deferred P6 physical move relocates them into `@chronicles/contracts`. The cruiser rule already forbids the components reaching domain/application/infrastructure/composition/server + the server SDKs/DB drivers today.

---

## Open decisions for Andrew (from spec §"Open Questions")
1. `[!]` Topology A (in-process route handlers) vs B (standalone server) — recommend **A** unless CDN-hosted client planned.
2. `[!]` **Mongo hosting: Atlas vs Railway replica set** — highest-stakes, blocks P3. Recommend Atlas if Phase-2 memory/vector search is on the horizon.
3. `[!]` Embeddings store (coupled to #2) — port + no-op adapter ship now regardless.
4. `[!]` Confirm npm workspaces (recommended); Turborepo deferred.
5. `[!]` Archivist carve aggressiveness — recommend staging; don't extract merge path + scene path in one PR.
6. `[!]` Version-of-record post-split — recommend `apps/web` + CI assertion.
7. `[!]` Overfit-data generalization timing — recommend dedicated PR, never on a "no behavior change" refactor.
8. `[!]` Dual-read verification depth before cutover (N / coverage).

---

## Status Log
_(newest first — agents append a line per phase gate with real command output)_

- 2026-06-04 — **P7 (enforce boundaries in CI — TOOLING ONLY) GATE GREEN.** Net-new boundary tooling; **no production deletion** (the SQLite adapter / `migrations.ts` / `better-sqlite3` / `serverExternalPackages` are UNTOUCHED — that `[!]` box stays unchecked, it is Andrew's after-prod-soak gate). (1) **`dependency-cruiser@^16.10.4`** (root devDep) + **`.dependency-cruiser.cjs`** at repo root with 11 forbidden rules path-scoped to the ACTUAL current layout (client tree = `packages/server/src/components`; the P6 physical `apps/web` move is deferred, so the client→contracts-only boundary is enforced in place). Rules: domain may not import application/infrastructure/composition/server/app/components nor `next`/`ai`/`@ai-sdk/*`/`mongoose`/`better-sqlite3`/`node:fs`; application may import only domain + sibling use-cases + `node:crypto`; only composition imports a concrete adapter; `mongoose` confined to `infrastructure/persistence/mongo/`; `better-sqlite3` confined to the sqlite adapter; domain/application may not reach `infrastructure/llm`; the client tree may not reach the onion/server layers nor `mongoose`/`better-sqlite3`/`ai`/`@ai-sdk/anthropic`/`@ai-sdk/xai`; `@chronicles/contracts` depends on `zod` only; no runtime circular deps. Aliases resolved via `tsconfig.depcruise.json` (minimal, alias-only, no `include` enumeration); `tsPreCompilationDeps: false` so only RUNTIME imports are graphed (erased `import type` cannot leak a bundle/cycle — this also drops the three known erased domain↔lib type back-edges). KNOWN deferred carves encoded as narrowly-scoped, commented allow-exceptions (NOT rule relaxations): `infrastructure/llm` model-registry/pricing imported by the 8 `lib/*` LLM agents by design; `app/api/chat/route.ts` injecting `narrateTurn` directly (P5 deferred); `better-sqlite3` allowlist `lib/db.ts`/`migrations.ts`/`archivist.ts` (P1/P4 strangling incomplete) — all in P7 itemsRemaining. Non-vacuity verified by injecting a temp `domain→composition` import (fired `domain-points-inward`) then restoring. (2) **Guard-grep tests** `tests/architecture-boundaries.test.ts` (6, in the default suite, comment-stripping): no `claude-`/`grok-` literal outside `infrastructure/llm/` (ZERO exceptions); no `import 'mongoose'` outside the mongo adapter (ZERO); no NEW merge/name-resolution deciding branch under `infrastructure/` (ZERO); `better-sqlite3` only in the sqlite adapter + pinned deferred allowlist; every infra module + route under `server-only`. Non-vacuity verified (temp `claude-…` literal fired the model-ID test) then restored. (3) **`import 'server-only'`** added to all 8 API route handlers (chat/usage/world-corrections/tts/turns/world-correction/world-state/tts-record); all 14 infra dirs already carried it; `mongo/test-support.ts` deliberately excluded (test-harness-only). (4) **Wired into CI**: root `pretest` hook runs `npm run depcruise` before vitest, so the default `npm test` enforces boundaries first then reports the vitest counts. Real gate output from repo root:
  - `npm run type-check` → `tsc --noEmit` clean (no errors)
  - `npm test` → `pretest`→`depcruise` `✔ no dependency violations found (169 modules, 218 dependencies cruised)`, then vitest `Test Files 37 passed | 1 skipped (38)` / `Tests 367 passed | 1 skipped (368)` — 361+1 baseline preserved + 6 new architecture-boundary guards
  - `npm run depcruise` → `✔ no dependency violations found (169 modules, 218 dependencies cruised)`, exit code 0
  - `npm run test:mongo` → `Test Files 3 passed (3)` / `Tests 16 passed (16)` — untouched, still green
  - `npm run build` → `✓ Compiled successfully`, all 10 API routes + pages built (one benign `no-unused-vars` warning), client bundle clean (the new route `server-only` markers do not leak into Chat.tsx)
  - **DEFERRED (Andrew's `[!]` gate, untouched):** delete SQLite adapter + `migrations.ts` + `better-sqlite3` dep + drop `serverExternalPackages` — only after Mongo runs clean in prod for a sustained window.
- 2026-06-04 — **P3 SCRIPTS (code-only deliverables) GATE GREEN.** Wrote the two idempotent maintenance scripts in `packages/server/scripts/`; **NEITHER was executed against any database** (the prod cutover + Atlas-vs-Railway hosting decision are Andrew's `[!]` manual gates, untouched). Both reuse the P2 adapter write path — `connectMongo` + `buildModels` — so there is NO second schema and NO direct `mongoose` import (boundary grep clean; the adapter owns mongoose). (1) `migrate-sqlite-to-mongo.ts`: opens the better-sqlite3 file READ-ONLY (`DATABASE_PATH`, `fileMustExist`), connects Mongo (`DATABASE_URL`), inserts parents-before-children (worlds→places→scenes→characters→story_*→turns→reveries/intents/timeline/population/occupancy/tts/corrections). Honors the **as-built P2 schema** (denormalized integer `worldId` + monotone integer `id`/`seq` kept VERBATIM — no ObjectId-FK rewrite, because the shipped ports speak `number` ids byte-compatibly with SQLite autoincrement, and `[t:N]` provenance references the integer). Conversions: `place_profiles` rows folded into `places.profile`; `osm_*`→`geo` subdoc; TEXT-JSON `initial_state_json`/`metadata`/`traits_json`/`daily_loop`→native subdocs via `JSON.parse`; `match_tags` TEXT→`matchTagsJson` JSON-array string; audio BLOB→`Buffer` (BSON Binary); append-only `memorable_facts`/`observations`/`aliases` stay strings ([t:N] inside). `nameKey`/`titleKey` = `lower(name|title)` derived on insert. **Idempotent** via replaceOne(upsert) keyed on the stable integer id/seq. Seeds all 16 monotone counters (`turnSeq` + per-collection) at `MAX(id)` with a `$max` pipeline so a re-run after live inserts never regresses the allocator. `createIndexes()` runs AFTER bulk insert so a latent duplicate that SQLite's `lower(name)` UNIQUE silently prevented surfaces as E11000. (2) `verify-parity.ts`: opens both stores read-only, asserts per-collection exact row counts (16 collections), full turn `id`↔`seq` continuity (whole-array equality) + `MAX(seq)` parity + `counters.turnSeq ≥ MAX`, N-sampled deep-equal on random turns AND characters (N via `VERIFY_SAMPLE_N` env / argv, default 25), and a store-agnostic per-world `getFullWorldState`-shape assembly (worldTime/currentSceneId + ordered character/place/scene/thread ids) compared from BOTH stores; prints PASS/FAIL with the first 50 mismatches and exits non-zero on any diff. Each script carries the required "DO NOT run against prod without a fresh backup; run against a copy; createIndexes after insert" header + the `npx tsx --conditions=react-server` invocation (so the adapter's `server-only` marker resolves to its no-op). Scripts are type-checked (in the tsconfig `**/*.ts` include) but NOT added to the test suite or executed. Real gate output from repo root:
  - `npm run type-check` → `tsc --noEmit` clean (no errors; both scripts compile)
  - `npm test` (DEFAULT, sqlite) → `Test Files 36 passed | 1 skipped (37)` / `Tests 361 passed | 1 skipped (362)` — baseline byte-green and untouched (the scripts add no runtime behavior to the suite; default persistence stays sqlite)
  - **DEFERRED — Andrew's manual `[!]` gates (untouched):** Mongo hosting choice (Atlas vs Railway single-node replica set), prod SQLite backup via `railway ssh`, running the backfill against the backup, parity-until-clean, and the quiet-window cutover. None executed; no prod DB / railway touched.
- 2026-06-04 — **P2 (Mongo + Mongoose adapter behind a flag) GATE GREEN — verified against a REAL replica set.** Implemented the SAME repository ports (already in `domain/ports/`) with Mongoose in `infrastructure/persistence/mongo/`, default `PERSISTENCE=sqlite` untouched. `mongoose@^9.6.3` (dep) + `mongodb-memory-server` (devDep) installed. **connection.ts:** `connectMongo` fails fast if the deployment is not a replica set (probes the `hello` admin command for `setName`/`isdbgrid`); build-phase no-op returns a disconnected `createConnection()` under `NEXT_PHASE=phase-production-build` (the Mongo analog of the SQLite `:memory:` guard). **models/index.ts** (THE ONLY place `mongoose` is imported): 15 top-level collections + 2 embedded subdocs (places.geo + places.profile = the absorbed place_profiles 1:1, worlds.initialState) + a `counters` collection; CHECK→`enum`, `importance 1..5`/`intensity|confidence 0..1`→`min/max`, functional `lower(name|title)` uniques→normalized `nameKey`/`titleKey` with `{worldId,nameKey}`/`{worldId,titleKey}` unique compound indexes, `{worldId,turnId,modelKey,voiceId,textHash}` unique for tts, partial index `WHERE narratorTurnId IS NULL` for intents. **Monotone integer `seq`** via `counters` + atomic `findOneAndUpdate $inc` inside the turn-insert transaction (`MongoContext.nextSeq`, session-aware) — NEVER ObjectId for ordering; every collection's integer `id` is allocated the same way so the `number`-typed ports stay byte-compatible with SQLite autoincrement and `[t:N]` provenance survives. **TurnRepository.mongo** is append-only: `insert` (allocates seq in the txn), `mergeMetadata` via targeted `$set` on `metadata.<agentKey>` (additive per agent, NOT document replace), `incTtsChars` via `$inc` on `metadata.tts.chars`; NO general update/setMetadata/delete (guard test pins content/role/seq unchanged after insert). **mongo-unit-of-work.ts** = `session.withTransaction`, threading the session via `MongoContext.setSession` so the counter increment + dependent writes commit/rollback together; single-doc writes open no session. **Prune ported, not just inserts:** reverie cap-3 + dedup + repoint-before-delete (`reverie-repository.mongo`) and tts turn-based retention (`tts-cache-repository.mongo`) run inside the same `withTransaction` as the insert/upsert. (Occupancy snapshots are append-only in the SHIPPED SQLite path — no prune exists there — so the mongo insert stays append-only to keep the stores byte-identical; documented in code.) **MemoryRepository.searchSimilar** port (`domain/ports/memory-repository.ts`) + no-op `[]` adapters for BOTH stores (Phase-2 vector slot). **Composition root** reads `PERSISTENCE`: sync `getContainer()` for the sqlite default, async `initContainer()` for mongo (connect + `syncIndexes()` via a DYNAMIC `import()` so the sqlite path never loads mongoose). `mongoose` confined to `infrastructure/persistence/mongo/` (+ a type-only import in the test harness via the `connectTestMongo` factory inside the adapter dir) — boundary grep clean. The Mongo suite is isolated in `vitest.mongo.config.ts` + excluded from the default `vitest.config.ts` run, so the SQLite gate stays byte-identical and the default run never pulls a Mongo binary. **`MongoMemoryReplSet` IS available in this environment** (downloaded + ran MongoDB 8.2.6 as a single-node replica set — transactions honored), so the mongo suite ACTUALLY RAN and is gate-passed, not skipped. Real gate output from repo root:
  - `npm run type-check` → `tsc --noEmit` clean (no errors)
  - `npm test` (DEFAULT, sqlite) → `Test Files 36 passed | 1 skipped (37)` / `Tests 361 passed | 1 skipped (362)` — baseline byte-green, the default persistence + every existing test untouched
  - `npm run test:mongo` (`vitest.mongo.config.ts`, `PERSISTENCE=mongo`, real `MongoMemoryReplSet`) → `Test Files 3 passed (3)` / `Tests 16 passed (16)` — covers unique-index E11000 (character nameKey + thread titleKey), counters seq monotonicity under 25 concurrent inserts + global-across-worlds, metadata `$set`-nested additive merge (disjoint agents + re-merge-one-key), `$inc` tts.chars accumulation, append-only guard (no update/setMetadata/delete + content/role/seq immutable), reverie cap-3 eviction + normalize-dedup, tts turn-based retention prune + non-assistant-turn guard, UnitOfWork rollback-on-throw + atomic counter+insert commit, replica-set guard pass, build-phase no-op
  - `npm run lint` → 0 errors; `npm run build` → ✓ Compiled successfully, all 10 API routes built, mongoose absent from the client bundle (dynamic import + `server-only` on the adapter)
  - **DEFERRED to P3 (manual gate, scripts only):** the SQLite→Mongo backfill + parity scripts; live Mongo hosting choice; prod cutover. P2 is code-only and the flag defaults to sqlite, so nothing in prod changes.
- 2026-06-04 — **P5 PART 2 (the chat god endpoint) GATE GREEN.** Carved `AdvanceTurn` out of the 593-line `app/api/chat/route.ts` — the highest-risk piece in the refactor (teleport + replay-cost prod-bug history). Three independently-revertible commits, gate green after each. (1) `NarrationStream {chunks, completion}` value type (`domain/ports/narrator.ts`) + `BackgroundTasks` port (`register`/`drain`) + `ProcessBackgroundTasks` infra adapter owning the `process.once('SIGTERM')` drain + container wiring. (2) The carve: `application/use-cases/advance-turn.ts` owns the load-bearing structure — pre-stream fail-closed gates as domain errors (`WorldNotFoundError`→404 / `EmptyPlayerActionError`→400 / `BudgetExceededError {used,limit}`→429, mapped to HTTP ONLY at the route edge), the pure `decideReplay` retry/replay dedup decision, the PRE-stream player-turn persist via `TurnRepository.insert`, and `[t:N]` `playerTurnId` derivation — the ex-`onFinish` closure vars are now explicit state. Returns `AdvanceTurnResult` (canned meta/replay | `NarrationStream`) — never a framework `onFinish`. `infrastructure/narrator/narrate-turn.ts` is the NarratorPort adapter: it owns the AI-SDK (`streamText`/`onFinish`/`toUIMessageStream`) AND the dense SQL+SDK pipeline body (classify/geocode/npc-agent/occupancy/reverie-flare/prompt render + the post-stream factual work), registers the archivist promise on `BackgroundTasks`, and resolves `completion` at stream-flush so the route's `dbTurnId` metadata part lands last. The route is now a ~160-LOC thin parse→execute→pipe adapter (extracted `appendDbTurnId` helper). **PRESERVED EXACTLY:** player-turn-pre-stream/factual-post-stream ordering; fail-open vs fail-closed (npc-agent/occupancy/geocoder/reverie/archivist/dedup stay `console.error`+continue — use-case test pins archivist-throws → both turns persist); SIGTERM archivist drain; world-existence-before-body-parse 404 ordering; the `dbTurnId` TTS/cost protocol. `application/advance-turn.ts` grep-clean of `@/lib`/`@/infrastructure`/`ai`/`@ai-sdk`/`next`/`better-sqlite3`/model-IDs. (3) Tests: `tests/advance-turn.test.ts` (8, Map-backed fake repos, no DB/SDK) + `tests/chat-stream-metadata.test.ts` (3, metadata-arrives-last). **DEFERRED (carried, net-zero-behavior, revert-risk>reward):** the fused `applyArchivistPatch` merge txn (NameResolution/NpcPromotion MergePlan from P4) is still applied inline by the narrate adapter — not yet load-all→simulate→apply; `buildNarration` is one injected infra step, not fully decomposed into granular per-step use-case deps; browser end-to-end smoke (needs live xAI key). Real gate output from repo root:
  - `npm run type-check` → `tsc --noEmit` clean (no errors)
  - `npm test` → `Test Files 36 passed | 1 skipped (37)` / `Tests 361 passed | 1 skipped (362)` — P5-part-1 baseline (350+1) preserved + 11 new (8 advance-turn + 3 stream-metadata)
  - `npm run build` → `✓ Compiled successfully`, all 10 API routes built, client bundle unchanged (`server-only` on the new narrator + background adapters does not leak into Chat.tsx)
- 2026-06-04 — **P5 PART 1 (lower-risk routes) GATE GREEN.** Carved the six lower-risk route handlers into `application/use-cases/*` thin-adapter pairs, one independently-revertible commit each, gate green after every one. New use cases: `load-history.ts` (LoadHistory — TurnRepo reads; per-turn cost `summarizeTurn` stays in the route adapter since `turn-cost`→infra/pricing; finishes strangling `@/lib/db` out of `turns/route.ts`, `Turn`/`AssistantTurnMetadata` now from `@/domain/entities`), `summarize-usage.ts` (SummarizeUsage), `inspect-world.ts` (InspectWorld — gates existence via WorldRepo, runs the `getFullWorldState` projection injected as a fn; the projection's own SQL move is P6; `world-state/route.ts` no longer imports `@/lib/worlds.getWorld`), `list-corrections.ts` (ListCorrections), `synthesize-narration.ts` + `record-tts-usage.ts` (TTS — added `domain/ports/speech-synthesizer.ts` `SpeechSynthesizer` port + `infrastructure/tts/xai-speech-synthesizer.ts` adapter wrapping `lib/tts.ts`, wired as `speech` in the container; the use case owns cache-key derivation + cache-or-synthesize + size-capped persist, the byte `tee` + HTTP headers stay in the route), `apply-correction.ts` (ApplyCorrection — **PARTIAL**: orchestration + cost-fold carved, `WorldNotFound`/`CorrectionExtractFailed`/`CorrectionApplyFailed` mapped to 404/502/500 at the edge; the fused `applyArchivistPatch` merge txn + `extractCorrectionPatch` LLM call are injected as route-wired fns — the load-all→simulate→apply MergePlan rewrite is the AdvanceTurn work in P5 part 2). `WorldNotFoundError` is defined once in `load-history.ts` and re-exported by the others. `application/` imports `@/domain/*` + sibling use cases + `node:crypto` (pure hash) only — grep-clean of `@/lib/db`/`@ai-sdk`/`ai`/`next`/`better-sqlite3`/`@/infrastructure`. No new `@/lib/db` value-importers introduced; the remaining ones (`archivist`/`npc-*`/`reveries`/`worlds`/`place-resolver`/`intent-reconciler`/`place-population`/`world-state`/`meta-commands`/`opening-turn` + `app/api/chat/route.ts`) are all on the chat/archivist path, P5 part 2. Real gate output from repo root:
  - `npm run type-check` → `tsc --noEmit` clean (no errors)
  - `npm test` → `Test Files 34 passed | 1 skipped (35)` / `Tests 350 passed | 1 skipped (351)` — baseline preserved (P4's 350+1 held; no new tests this slice — use-case fake-repo tests are the P5-part-2 deliverable)
  - `npm run build` → `✓ Compiled successfully`, all 10 API routes built, client bundle unchanged (the new `infrastructure/tts` adapter carries `server-only`, no leak into Chat.tsx)
  - **NOT done (carried to P5 part 2):** `chat→AdvanceTurn` (the 594-line god endpoint + ~20 onFinish closure vars + fail-open/fail-closed + dbTurnId flush ordering + SIGTERM drain); the `applyArchivistPatch` fused merge-txn MergePlan carve (shared with NameResolution/NpcPromotion P4 partials); use-case tests against in-memory fake repos + FakeClock.
- 2026-06-04 — **P4 CARVE (the risky decisions) GATE GREEN.** Five independently-revertible commits, each gated. Two extractions FULLY landed as pure-service-returns-value/intent; two are HONEST PARTIALS (pure rules extracted, the fused-transaction "return a plan, issue no SQL" carve deferred to P5 — reverting risk > reward on a no-behavior-change pass; documented in itemsRemaining). Characterization tests written and green BEFORE/with each extraction; `archivist.test.ts` (incl. the world-13 teleport + Call-In turn-403 scenarios) stayed byte-green throughout. Commits, newest first: `9155971` SceneTransition invariant → `domain/services/scene-transition.ts` `decideSceneTransition()` returns a `SceneTransitionIntent`|null, txn applies it (own commit, NOT with merge path per Open-Q#5); `7005a41` NpcPromotion pure tiering rules (`nextAgencyTier`/`isTransientServiceNpc`) — orchestrator UPDATEs stay; `d02d951` NameResolution pure rules (`charactersMatch`/`placesMatch`/`freshest`/`mergeLineBlocks`/… ) — `resolveCharacter`/`mergeCharacters` SQL wrappers stay; `f3e06ff` narrator-markdown renderers → `src/server/render/state-block.ts` (rendering, not domain); `68ff9d1` PatchSanitizer pure domain service (`sanitizeArchivistPatch`/`extractDeterministicPatch`/`normalizeTransitPlaceName`). `domain/` imports none of ai/@ai-sdk/next/better-sqlite3/@/lib/db (grep clean); the two domain↔lib type leaks (`ArchivistPatch`, `CharacterRow`/`PlaceRow`, `NarratorWorldState`) are `import type` only (fully erased — no runtime cycle). archivist.ts shrank 2224→1658 LOC. Real gate output from repo root:
  - `npm run type-check` → `tsc --noEmit` clean (no errors)
  - `npm test` → `Test Files 34 passed | 1 skipped (35)` / `Tests 350 passed | 1 skipped (351)` — 323 baseline preserved + 28 new (20 name-resolution + 8 scene-transition)
  - `npm run build` → `✓ Compiled successfully`, routes built, client bundle clean (`server-only` on the render layer does not leak into Chat.tsx). Not part of the hard gate but green.
  - **NOT done (carried to P5):** the full MergePlan/write-command carve of `mergeCharacters`/`runAliasMerges`/`mergePlaces`/`recordAppearancesAndAutoPromote` — they still issue UPDATE/DELETE/RETURNING interleaved inside `applyArchivistPatch`'s 370-line transaction; converting to load-all→simulate→apply is the `AdvanceTurn` use-case rewrite of the fused txn (prod-bug history). Also still deferred: the overfit `minerva/caesar/maya/usace` generalization (behavior change, separate PR).
- 2026-06-04 — **P4 FOUNDATION (low-risk moves) GATE GREEN.** Three independently-revertible commits, zero behavior change: (1) `9df09cc` row TYPE defs → `domain/entities/*` (World/Turn+metadata/Character/Place/Scene/Story*/NpcIntent/Reverie/Occupancy/Usage/TtsCache/Correction); 6 repository ports re-pointed off `@/lib/db` → `@/domain/entities` (the last domain→infra type leak is gone — `grep` confirms no `@/lib/db` import anywhere under `domain/`); `db.ts`/`world-state.ts`/`worlds.ts`/`npc-intents.ts`/`reveries.ts` re-export the names for back-compat. (2) `1b443f1` pure drop-in service moves into `domain/services/`: `world-clock`, `turn-numbering`, `story-signal`, `memorable-fact-provenance`, `character-dedup`, `reverie-flare` (pure core; reveries CRUD stays), `occupancy-sim` (pure core; `buildPlaceOccupancySnapshot` orchestrator stays in lib), `action-classifier-rules` (rules core; LLM fallback stays, rules-first order preserved), `narrator-guidance` (moved as-is — overfit generalization deferred). Public signatures preserved; old `@/lib/*` modules re-export. `domain/` imports none of the forbidden libs (ai/@ai-sdk/next/better-sqlite3/@/lib/db); `character-dedup` still imports the pure `@/lib/character-identity` (not on the banned list). (3) `51775a5` model-registry consolidation: `infrastructure/llm/model-registry.ts` (server-only) is the single home for `grok-4.3` + `claude-haiku-4-5-20251001`; `pricing.ts` → `infrastructure/llm/pricing.ts` (server-only, RATES keyed by registry consts); `lib/pricing.ts` slimmed to client-safe `formatUsd`; `turn-cost.ts` reads cost math from infra; all 8 call sites import the registry — zero model-ID literals remain outside it. Real gate output from repo root:
  - `npm run type-check` → `tsc --noEmit` clean (no errors)
  - `npm test` → `Test Files 32 passed | 1 skipped (33)` / `Tests 322 passed | 1 skipped (323)` — baseline preserved
  - `npm run build` → `✓ Compiled successfully`, 12 routes, client bundle clean (verifies `server-only` does not leak into Chat.tsx through the pricing split). Not part of the hard gate but green.
  - **NOT done in this slice** (remaining P4): characterization tests for NameResolution/SceneTransition/applyArchivistPatch ordering; the higher-risk extractions (PatchSanitizer, SceneTransition intents, NameResolution MergePlan, NpcPromotion, narrator-markdown renderers→server/render); the deferred overfit-generalization PR.
- 2026-06-04 — **P1 PARTIAL, GATE GREEN.** Recovered an uncommitted prior-session P1 build that had left the suite RED: `tts-route-warm.test.ts` failed because the migrated route → container → infra chain pulls `import 'server-only'`, whose default export throws outside an RSC boundary (Vitest has none). Fixed by aliasing `server-only` → its no-op `empty.js` in `packages/server/vitest.config.ts` (the same file Next resolves under the `react-server` condition). That restored 323. Then fully strangled `cost-cap.ts`: its raw `SUM(json_extract(...))` over `db` now goes through `UsageRepository.todaysTokenTotal(clock.today())` (SQL already lived in `usage-repository.sqlite.ts`); `todaysTokens`/`isOverDailyLimit` became async; updated 3 callers (`chat/route.ts` cap gate, `opening-turn.ts`, `cost-cap.test.ts`). Ports (14 + Clock/Logger), all-async signatures, append-only `TurnRepository`, single SQLite adapter set, `composition/container.ts`, and `server-only` guards were all present from prior work and verified. **NOT done:** the remaining 12 VALUE importers of `@/lib/db` (SQL-owning lib modules — archivist/reveries/npc-*/world-state/worlds/place-resolver/intent-reconciler/meta-commands/opening-turn — whose SQL is wrapped behind adapters that delegate back into them, plus the P5 `chat/route.ts` god endpoint). The exit-criterion guard-grep was deliberately NOT added because the invariant it asserts does not yet hold; adding it would fail the suite. Real gate output from repo root:
  - `npm run type-check` → `tsc --noEmit` clean (no errors)
  - `npm test` → `Test Files 32 passed | 1 skipped (33)` / `Tests 322 passed | 1 skipped (323)` — baseline preserved (the 1 skip is pre-existing)
- 2026-06-04 — **P0 GATE GREEN.** App moved verbatim under `packages/server/` via `git mv` (history-preserving renames); npm workspaces stood up (`packages/*`, `apps/*`), root scripts delegate to `@chronicles/server`; `tsconfig.base.json` added, server tsconfig extends it (keeps `@/*`, Next plugin, `serverExternalPackages: ['better-sqlite3']`); cwd coupling removed in `db.ts` + `prompt-files.ts` (module-relative via `import.meta.url`). Real gate output from repo root after `npm install`:
  - `npm run type-check` → `tsc --noEmit` clean (no errors)
  - `npm test` → `Test Files 32 passed | 1 skipped (33)` / `Tests 322 passed | 1 skipped (323)` — matches baseline 33 files / 323 tests
  - `npm run build` → `✓ Compiled successfully`, page data collected, 12 routes built (one benign warning: "TypeScript project references are not fully supported", falls back to incremental). Not part of the hard gate but green.
- 2026-06-04 — Branch `onion-arch-refactor` created. Baseline captured: type-check clean, 323 tests pass. Tracker initialized.
