# Milestone: v0.5.0 — Narrator Controls & Story Continuity

**Status:** implemented on branch (not yet promoted to `production`)
**Date:** 2026-08-09
**Version:** `0.5.0` (MINOR)
**Plan:** `docs/plans/narrator-controls-story-continuity.md` (archive on merge)

## What shipped

- **PR A** — Purchase / synonym inventory mint (word-boundary + proximity + currency guard)
- **PR B** — Single protagonist identity (session seed, STATE pin, correction channel)
- **PR C** — Open-world narrative clock (deterministic primary; LLM jump-only; band-only open render)
- **PR D** — Dossier ranking (R8) + PRIMARY PRESSURE pin + idle primary pressure guidance
- **PR F** — Descriptor continuity / archivist rules
- **PR E (option a)** — `shouldEscalateViolence` pure helper + grounded cost prompts
- **Replay fixture** — Cluster Psi-1 offline reducer tests (no LLM)

**Out of this code drop:** PR 0 Cluster Psi-1 prod data repair (ops one-shot after B).

## Exit criteria

- [x] Workspace versions `0.5.0` (root, server, contracts, lockfile) on the release branch
- [x] `RELEASES` entry newest-first in `packages/server/src/components/release-notes/data.ts`
- [x] Unit tests + depcruise green (`npm test`)
- [ ] Restart `npm run dev` and confirm header on `/` shows `0.5.0`
- [ ] Merge → `main`, promote `main` → `production` when ready to deploy
- [ ] Confirm header + What’s New on deployed host
- [ ] Optional: run PR 0 data repair against Cluster Psi-1 after B is live
