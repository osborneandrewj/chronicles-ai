// Moved to domain/services/character-identity.ts (A0 — domain may not import lib).
// Re-exported here for back-compat with existing `@/lib/character-identity` importers.
export { isDescriptorName, nameKey } from '@/domain/services/character-identity'
