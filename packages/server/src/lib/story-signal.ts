// Moved to domain/services/story-signal.ts (P4 pure-service relocation).
// Re-exported here for back-compat with existing `@/lib/story-signal` importers.
export { hasRichStorySignal } from '@/domain/services/story-signal'
