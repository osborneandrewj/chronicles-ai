// Moved to domain/services/turn-numbering.ts (P4 pure-service relocation).
// Re-exported here for back-compat with existing `@/lib/turn-numbers` importers.
export { buildTurnNumberMap } from '@/domain/services/turn-numbering'
