// Moved to domain/services/narrator-guidance.ts (P4 pure-service relocation;
// moved as-is — the minerva/caesar/maya/usace overfit generalization is a
// deferred behavior-change PR). Re-exported here for back-compat with existing
// `@/lib/narrator-guidance` importers.
export { formatNarratorTurnGuidance } from '@/domain/services/narrator-guidance'
